{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write .",
    "format-check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^2.8.1"
  }
}
